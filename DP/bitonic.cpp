#include<bits/stdc++.h>
using namespace std;
//void solution(){
//}
int calculate_bitonic(int i,int flag,vector<int>&arr,vector<vector<int>>&dp,int n){
   if(i>=n)return 0;
   if(dp[i][flag]!=-1)return dp[i][flag];
      if()
    }
    return dp[i]=ans;
}
int main (){
long long int n,k;
cin >> n >> k;
vector<vector<int>> dp(n,vector<int>(2,-1));

return 0;
}